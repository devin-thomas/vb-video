# Ready-to-paste dispatch prompts

## Coordinator kickoff
```text
Read README.md, AGENTS.md, manifest.json, docs/PRODUCTION_BIBLE.md, docs/OUTPUT_CONTRACT.md, and docs/EDITORIAL_REGISTER.md.
Execute this pack as an asset-production project, not a script rewrite. Start OPS-01 (shared visual/export system), OPS-02 (isolated .NET harness), and OPS-03 (editorial/evidence ledger) in independent lanes where the environment permits.
Use one worker assignment per ticket ID. Respect per-ticket write ownership and dependencies. Do not load all 134 tickets into every worker. All initial state files are planned; no media is already generated.
Dispatch independent tickets after their needed shared inputs are usable. Researchers may collect candidates while layout infrastructure is prepared. Keep proof-production status separate from release approval. Never invent screenshots, run output, sources, rights, or approvals.
Use tools/dispatch.py to inspect readiness, then give each worker its full tickets/<ID>.md and relevant source files. Track status only in each asset's own state.json. Report actual progress and specific blockers. Finish with OPS-04 and the H01–H07 handoffs; do not upload or publish without explicit authorization.
```

## One asset worker
```text
Your single assignment is tickets/CODE-02.md.
Read it completely, plus AGENTS.md and the named production/output/gate documents. Your write ownership is assets/CODE-02/ only. Consume the frozen shared system without editing it. Preserve sources/ byte-for-byte.
Create and inspect this particular asset, not a generic template standing in for it. Follow every exact source token, named variant, output requirement, and content-specific acceptance check. Record commands/tests that actually ran and any unresolved editorial or capability gate.
Update assets/CODE-02/state.json and return concrete deliverable paths, provenance, QA results, production status, and release status. Do not claim a completed PNG/MP4 without real files.
```
Replace every `CODE-02` occurrence with the chosen ID. The ticket contains the actual instructions; never dispatch from only a one-line manifest title.

## Research worker
```text
Your assignment is tickets/HIST-02.md. Verify the exact product/version/date, locate the underlying image source, inspect the image, and record item-specific provenance and rights. Public browsing is not clearance. Keep raw original and edited framing separate.
Do not substitute another VB version, invent an archive URL/license, recreate an old screenshot as documentary evidence, or perform unapproved login/payment/terms acceptance. A useful blocked candidate report is better than fake success. Own assets/HIST-02/ only; record the actual source URLs and access dates when execution occurs.
```

## Reviewer
```text
Review one completed ticket against its full work order and originals. Run the relevant validator, inspect actual media at full size and 720p, and audit exact code/text, diagrams, counts, provenance, rights, and release gates. File presence is not visual or factual correctness.
Record pass/fail with concrete evidence. Keep production review distinct from publication approval. Do not edit the original script or silently patch the program to make the output agree with narration.
```
