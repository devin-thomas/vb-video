---
id: "DIA-10"
title: "Game flowchart: setup, rounds, war loop, result"
owner_role: "design"
original_family: "T1-D10"
priority: "required"
production_status: "planned"
release_gates: ["R04", "R05", "R06"]
---

# DIA-10 — Game flowchart: setup, rounds, war loop, result

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Provide a legible overview and a code-faithful loop inset without reducing every possible result to a win.

**Original plan family:** `T1-D10` — Game flowchart (Build→Shuffle→Deal→Loop→Winner)

**Primary owner role:** design. **Type:** still. **Script section(s):** 9, 10. **Scope priority:** required.

**Method:** Editable SVG for diagrams; offline HTML/CSS for composed text/image layouts. Export PNG poster and named state variants.

**Owned write paths:** `assets/DIA-10/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../AGENTS.md), the [production bible](../docs/PRODUCTION_BIBLE.md), and the [output contract](../docs/OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:25–25` ([original](../sources/ASSET_PLAN.md))
- `SCRIPT.md:388–420` ([original](../sources/SCRIPT.md))
- `SCRIPT.md:460–489` ([original](../sources/SCRIPT.md))
- `Program.vb:44–94` ([original](../sources/Program.vb))
- `Program.vb:218–270` ([original](../sources/Program.vb))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Main path: Build → Shuffle → New hands → Deal → Both have cards? → increment round → above MAX_ROUNDS? → PlayRound → repeat.
2. The round-cap branch ends in draw handling. The both-have-cards false branch checks the hand counts before winner summary.
3. PlayRound inset shows empty-hand checks, draw pair, append to pot, compare, award on inequality, burn on tie, then repeat.
4. Do not depict a recursive PlayRound call or a true visited-state cycle detector. Source message wording is not proof of detection.
5. Split the overview and detail into separate same-ticket export variants rather than shrinking the full diagram below readable type.

## Exact copy / source payload
~~~~text
Build → Shuffle → Deal
Both have cards?
Round cap exceeded?
Play round
Winner / Draw
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. Overview still: setup and game loop with winner/draw exits.
2. Round-detail still: the inner Do loop and tie/burn return arrow.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `main-flow`, `playround-detail`

## Deliverables
Paths below are relative to `assets/DIA-10/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `evidence/claim-checks.json`
- `src/scene.svg`
- `exports/poster.png`
- `exports/main-flow.png`
- `exports/playround-detail.png`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] Both “No cards” and cap/draw exits exist.
- [ ] Loop-back arrows are directed and do not cross node labels.
- [ ] Empty-hand checks precede draws.
- [ ] A 20,000 cap is shown without claiming the source summary counter is always the number of completed rounds.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
- **R04** — read the corresponding entry in [EDITORIAL_REGISTER.md](../docs/EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.
- **R05** — read the corresponding entry in [EDITORIAL_REGISTER.md](../docs/EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.
- **R06** — read the corresponding entry in [EDITORIAL_REGISTER.md](../docs/EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:25–25

~~~~text
| 10 | Game flowchart (Build→Shuffle→Deal→Loop→Winner) | SVG flowchart |
~~~~
### SCRIPT.md:388–420

~~~~text
The main game loop is where everything comes together. Let's walk through `Sub Main`:

```vb
BuildDeck(Deck)
ShuffleDeck(Deck)

Player1 = NewHand()
Player2 = NewHand()
DealCards(Deck, Player1, Player2)
```

We build a deck, shuffle it, create two empty hands, and deal — alternating cards, one to Player 1, one to Player 2, until all 52 are distributed. Each player gets 26.

**[VISUAL: Animation — a deck splitting into two piles, cards alternating left and right.]**

Then the loop:

```vb
Do While Player1.Count > 0 And Player2.Count > 0
    RoundNumber = RoundNumber + 1
    If RoundNumber > MAX_ROUNDS Then
        ' Safety valve — call it a draw
        Exit Do
    End If
    PlayRound(RoundNumber, Player1, Player2, WarCount)
Loop
```

`Do While...Loop`. In C, that's `while (...) { }`. It keeps going as long as both players have at least one card. When either player hits zero, the loop ends and we have a winner.

**[VISUAL: Flowchart — "Both have cards?" → Yes → Play round → loop back. No → Determine winner.]**

The `MAX_ROUNDS` check is a safety valve. War is theoretically capable of cycling forever — the same cards going back and forth in a loop. In practice, with a random shuffle, games almost always end in a few hundred rounds. But just in case, we cap it at 20,000 rounds and call it a draw. In my testing, I've never actually hit the cap.
~~~~
### SCRIPT.md:460–489

~~~~text
Then there's an inner `Do...Loop` — not a `Do While`, just `Do...Loop`, which is an infinite loop that we exit with `Exit Sub` when someone wins. Each iteration:

One — check if either player is out of cards. If you can't play, you lose, and the other player takes the pot.

Two — both players draw their top card. Both cards go into the pot.

Three — compare ranks.

If Player 1's rank is higher, Player 1 wins the pot. `Exit Sub`.
If Player 2's rank is higher, Player 2 wins the pot. `Exit Sub`.
If they're equal? War.

```vb
WarNumber = WarNumber + 1
WarCount = WarCount + 1

BurnCount = Math.Min(3, Math.Min(Player1.Count, Player2.Count))
For i = 1 To BurnCount
    Pot(PotCount) = DrawTopCard(Player1) : PotCount = PotCount + 1
    Pot(PotCount) = DrawTopCard(Player2) : PotCount = PotCount + 1
Next i
```

**[VISUAL: Step-by-step animation of a War sequence:]**
**Step 1: Two equal cards face up.**
**Step 2: Three face-down cards from each player.**
**Step 3: One face-up card from each player.**
**Step 4: Higher card takes everything.**

Each player burns up to three cards face-down — or fewer, if they don't have three left. Then the loop goes back to the top, draws again, and compares again. If it's another tie, another war. The pot keeps growing.
~~~~
### Program.vb:44–94

~~~~text
        BuildDeck(Deck)
        ShuffleDeck(Deck)

        Player1 = NewHand()
        Player2 = NewHand()
        DealCards(Deck, Player1, Player2)

        Console.WriteLine("Cards dealt: Player 1 has " & Player1.Count & _
            ", Player 2 has " & Player2.Count & ".")
        Console.WriteLine()

        RoundNumber = 0
        WarCount = 0
        TheWinner = 0

        Do While Player1.Count > 0 And Player2.Count > 0
            RoundNumber = RoundNumber + 1

            If RoundNumber > MAX_ROUNDS Then
                Console.WriteLine()
                Console.WriteLine("No winner after " & MAX_ROUNDS & _
                    " rounds - calling it a draw (deck cycle detected).")
                TheWinner = 0
                Exit Do
            End If

            PlayRound(RoundNumber, Player1, Player2, WarCount)
        Loop

        Console.WriteLine()
        Console.WriteLine("=====================================")

        If Player1.Count = 0 And Player2.Count > 0 Then
            TheWinner = 2
        ElseIf Player2.Count = 0 And Player1.Count > 0 Then
            TheWinner = 1
        End If

        Select Case TheWinner
            Case 1
                Console.WriteLine("PLAYER 1 WINS THE WAR!")
            Case 2
                Console.WriteLine("PLAYER 2 WINS THE WAR!")
            Case Else
                Console.WriteLine("THE WAR ENDS IN A DRAW.")
        End Select

        Console.WriteLine("Total rounds played : " & RoundNumber)
        Console.WriteLine("Total wars fought    : " & WarCount)
        Console.WriteLine("=====================================")
    End Sub
~~~~
### Program.vb:218–270

~~~~text
        Console.Write("Round " & RoundNumber & ": ")

        Do
            ' A player who has run out of cards mid-war loses immediately.
            If Player1.Count = 0 Then
                Console.WriteLine("Player 1 has no cards left for the war - Player 2 takes the pot.")
                GiveCardsToWinner(Pot, PotCount, Player2)
                Exit Sub
            End If

            If Player2.Count = 0 Then
                Console.WriteLine("Player 2 has no cards left for the war - Player 1 takes the pot.")
                GiveCardsToWinner(Pot, PotCount, Player1)
                Exit Sub
            End If

            Card1 = DrawTopCard(Player1)
            Card2 = DrawTopCard(Player2)
            Pot(PotCount) = Card1 : PotCount = PotCount + 1
            Pot(PotCount) = Card2 : PotCount = PotCount + 1

            If WarNumber = 0 Then
                Console.WriteLine("Player 1 plays " & CardText(Card1) & _
                    ", Player 2 plays " & CardText(Card2) & ".")
            Else
                Console.WriteLine("        War card - Player 1 plays " & CardText(Card1) & _
                    ", Player 2 plays " & CardText(Card2) & ".")
            End If

            If Card1.Rank > Card2.Rank Then
                Console.WriteLine("        Player 1 wins the round (" & PotCount & " cards).")
                GiveCardsToWinner(Pot, PotCount, Player1)
                Exit Sub

            ElseIf Card2.Rank > Card1.Rank Then
                Console.WriteLine("        Player 2 wins the round (" & PotCount & " cards).")
                GiveCardsToWinner(Pot, PotCount, Player2)
                Exit Sub

            Else
                ' Tie: it's WAR. Each side burns up to 3 face-down cards,
                ' then both flip a new card to compare.
                WarNumber = WarNumber + 1
                WarCount = WarCount + 1
                Console.WriteLine("        ** WAR! ** (war number " & WarNumber & " this round)")

                BurnCount = Math.Min(3, Math.Min(Player1.Count, Player2.Count))
                For i = 1 To BurnCount
                    Pot(PotCount) = DrawTopCard(Player1) : PotCount = PotCount + 1
                    Pot(PotCount) = DrawTopCard(Player2) : PotCount = PotCount + 1
                Next i
            End If
        Loop
~~~~

## Production delivery — this batch

**Media/source production: produced. Publication status: blocked.**

[Individual delivery inventory](../assets/DIA-10/delivery.json) · [QA and remaining decisions](../assets/DIA-10/qa.md) · [Live state](../assets/DIA-10/state.json)

The work order above remains unchanged. See the package gallery for rendered previews. Production does not clear an unresolved historical or editorial gate.
