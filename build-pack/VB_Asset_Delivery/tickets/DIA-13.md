---
id: "DIA-13"
title: "Visual development lineage / influence graphic"
owner_role: "design"
original_family: "T1-D13"
priority: "required"
production_status: "planned"
release_gates: ["R13"]
---

# DIA-13 — Visual development lineage / influence graphic

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Create the requested visual-development lineage proof, while preventing unsupported causal arrows from becoming published history.

**Original plan family:** `T1-D13` — VB influence lineage diagram

**Primary owner role:** design. **Type:** still. **Script section(s):** 15. **Scope priority:** required.

**Method:** Editable SVG for diagrams; offline HTML/CSS for composed text/image layouts. Export PNG poster and named state variants.

**Owned write paths:** `assets/DIA-13/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../AGENTS.md), the [production bible](../docs/PRODUCTION_BIBLE.md), and the [output contract](../docs/OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:28–28` ([original](../sources/ASSET_PLAN.md))
- `SCRIPT.md:709–711` ([original](../sources/SCRIPT.md))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Include source-requested nodes VB, Delphi, .NET WinForms, WPF, and modern frameworks. Keep React, SwiftUI, and Flutter as optional source-mentioned examples only.
2. Draft the supplied sequence in a separate working layer and maintain evidence status for every edge.
3. Do not substitute a verified family tree with guessed influence. Edge labels must distinguish direct technical lineage, shared RAD ideas, and unverified script assertions.
4. A neutral “shared visual-development ideas” layout is a proposed fallback, not an automatic rewrite; producer approval is required.

## Exact copy / source payload
~~~~text
Visual development ideas
VB · Delphi · WinForms · WPF · Modern frameworks
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. Still overview with large readable node labels and a legend for edge meaning.
2. Optional focus overlay highlights the shared idea rather than adding unproven chronological direction.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `overview`

## Deliverables
Paths below are relative to `assets/DIA-13/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `evidence/claim-checks.json`
- `src/scene.svg`
- `exports/poster.png`
- `exports/overview.png`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] Every published edge has a evidence note and an approved relationship label.
- [ ] No causal arrow is released only because it appeared in the draft narration.
- [ ] A framework’s node does not imply VB language/runtime inheritance.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
- **R13** — read the corresponding entry in [EDITORIAL_REGISTER.md](../docs/EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.

## Ticket-specific notes
Do not solve this ticket by silently turning every arrow into a factual claim. Proof production may proceed; final release waits on R13.

## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:28–28

~~~~text
| 13 | VB influence lineage diagram | SVG — node-and-edge tree |
~~~~
### SCRIPT.md:709–711

~~~~text
But the ideas it pioneered — visual form designers, event-driven programming, RAD tools, components you can drop onto a surface and configure through properties — those ideas won completely. Every modern UI framework, from React to SwiftUI to Flutter, carries DNA that traces back to Alan Cooper's prototype and Microsoft's Visual Basic.

**[VISUAL: A lineage diagram showing VB's influence: VB → Delphi → .NET WinForms → WPF → modern frameworks. The form-designer concept flowing through all of them.]**
~~~~
