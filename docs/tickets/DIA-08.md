---
id: "DIA-08"
title: "War mechanic: tie, burn, flip, award"
owner_role: "motion"
original_family: "T1-D08"
priority: "required"
production_status: "planned"
release_gates: ["R05"]
---

# DIA-08 — War mechanic: tie, burn, flip, award

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Produce the requested four-panel war sequence plus a matching animated progression.

**Original plan family:** `T1-D08` — War mechanic step-by-step

**Primary owner role:** motion. **Type:** motion. **Script section(s):** 5, 10. **Scope priority:** required.

**Method:** Offline HTML/CSS/inline SVG with deterministic JavaScript seeking; exported review MP4 and PNG stills.

**Owned write paths:** `assets/diagrams/DIA-08/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:23–23` ([original](../../sources/ASSET_PLAN.md))
- `SCRIPT.md:188–192` ([original](../../sources/SCRIPT.md))
- `SCRIPT.md:483–489` ([original](../../sources/SCRIPT.md))
- `Program.vb:234–268` ([original](../../sources/Program.vb))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Use the single-war fixture: 4♠ versus 4♦, three face-down burns each, 9♦ versus A♦, Player 2 collects 10.
2. Show the initial two cards stay in the pot while six burns and two deciding cards are added.
3. Keep burn faces hidden in the rendered scene even though the fixture records identities for validation.
4. Export a four-panel poster as well as a 12-second motion; a repeat-tie arrow may connect to DIA-09 but must not be called a recursive function.
5. For the section-10 award cut, move pot cards one at a time in stored play order into the winner’s bottom; expose this as the award-to-bottom variant.

## Exact copy / source payload
~~~~text
1. Tie
2. Three face-down each
3. One face-up each
4. Higher rank takes the pot
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. 0–3 s: panel 1, equal ranks, pot 2.
2. 3–6 s: panel 2, six burns, pot 8.
3. 6–9 s: panel 3, deciding cards, pot 10.
4. 9–12 s: panel 4, ten cards append to Player 2.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `four-panel-poster`, `single-war-motion`, `award-to-bottom`

## Deliverables
Paths below are relative to `assets/diagrams/DIA-08/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `evidence/claim-checks.json`
- `src/index.html`
- `src/timeline.json`
- `exports/preview.mp4`
- `exports/poster.png`
- `exports/keyframes/start.png`
- `exports/keyframes/middle.png`
- `exports/keyframes/end.png`
- `exports/four-panel-poster.png`
- `exports/single-war-motion.png`
- `exports/award-to-bottom.png`
- `exports/single-war-motion.mp4`
- `exports/award-to-bottom.mp4`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] There are three face-down cards per player, not three total.
- [ ] The deciding pair is additional to the burns.
- [ ] Final counts are Player 1=21, Player 2=31, pot=0 from a 26/26 start.
- [ ] No short-hand case is claimed to be covered by the ordinary example.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
- **R05** — read the corresponding entry in [EDITORIAL_REGISTER.md](../EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:23–23

~~~~text
| 8 | War mechanic step-by-step | SVG — 4-panel sequence |
~~~~
### SCRIPT.md:188–192

~~~~text
Three. If both players flip the same rank — two sevens, two kings, whatever — it's War. Each player places three cards face-down, then flips a fourth card face-up. Whoever's face-up card is higher takes the entire pot — all ten cards. If it's another tie, you do it again. War can chain.

**[VISUAL: Animation — two equal cards appear, then three face-down cards from each player, then two more face-up cards. The pot grows.]**

Four. You keep playing until one player has all 52 cards. That player wins. If a player can't put up enough cards for a war, they lose.
~~~~
### SCRIPT.md:483–489

~~~~text
**[VISUAL: Step-by-step animation of a War sequence:]**
**Step 1: Two equal cards face up.**
**Step 2: Three face-down cards from each player.**
**Step 3: One face-up card from each player.**
**Step 4: Higher card takes everything.**

Each player burns up to three cards face-down — or fewer, if they don't have three left. Then the loop goes back to the top, draws again, and compares again. If it's another tie, another war. The pot keeps growing.
~~~~
### Program.vb:234–268

~~~~text
            Card1 = DrawTopCard(Player1)
            Card2 = DrawTopCard(Player2)
            Pot(PotCount) = Card1 : PotCount = PotCount + 1
            Pot(PotCount) = Card2 : PotCount = PotCount + 1

            If WarNumber = 0 Then
                Console.WriteLine("Player 1 plays " & CardText(Card1) & _
                    ", Player 2 plays " & CardText(Card2) & ".")
            Else
                Console.WriteLine("        War card - Player 1 plays " & CardText(Card1) & _
                    ", Player 2 plays " & CardText(Card2) & ".")
            End If

            If Card1.Rank > Card2.Rank Then
                Console.WriteLine("        Player 1 wins the round (" & PotCount & " cards).")
                GiveCardsToWinner(Pot, PotCount, Player1)
                Exit Sub

            ElseIf Card2.Rank > Card1.Rank Then
                Console.WriteLine("        Player 2 wins the round (" & PotCount & " cards).")
                GiveCardsToWinner(Pot, PotCount, Player2)
                Exit Sub

            Else
                ' Tie: it's WAR. Each side burns up to 3 face-down cards,
                ' then both flip a new card to compare.
                WarNumber = WarNumber + 1
                WarCount = WarCount + 1
                Console.WriteLine("        ** WAR! ** (war number " & WarNumber & " this round)")

                BurnCount = Math.Min(3, Math.Min(Player1.Count, Player2.Count))
                For i = 1 To BurnCount
                    Pot(PotCount) = DrawTopCard(Player1) : PotCount = PotCount + 1
                    Pot(PotCount) = DrawTopCard(Player2) : PotCount = PotCount + 1
                Next i
~~~~

## Production delivery — this batch

**Media/source production: produced. Publication status: blocked.**

[Individual delivery inventory](../../assets/diagrams/DIA-08/delivery.json) · [QA and remaining decisions](../../assets/diagrams/DIA-08/qa.md) · [Live state](../../assets/diagrams/DIA-08/state.json)

The work order above remains unchanged. See the package gallery for rendered previews. Production does not clear an unresolved historical or editorial gate.

## Revision 2 — 2026-09-16 (Devin's approval of proposal 4 (25:43))

**Status:** production back to `planned`; release back to `unreviewed` until the producer approves the new exports. Work on branch `ticket/DIA-08-r2`; write only inside the ticket's owned paths. Keep every requirement above unless this section changes it.

Add an `endgame` variant that tells the specific ending the narration walks through, with the real numbers: Player 2 holds 2 cards against Player 1's 50; both play a 10 (1 left against 49); both burn 1 (0 left against 48); Player 2 cannot flip, Player 1 takes the 4-card pot. Four panels, each landing on its sentence, with the counters shown; record each panel's second in delivery.json. Verify the numbers against the 617-round run's captured stdout (the same source FACT-07 and FACT-08 use) and cite the lines. Use the revised shared card faces (OPS-01). The existing generic war sequence stays as `preview`.

**Origin:** [review/cut-notes-2026-09-16.md](../../review/cut-notes-2026-09-16.md). Report as for any ticket.
