---
id: "DIA-04"
title: "ByRef versus ByVal: caller variable and local parameter"
owner_role: "motion"
original_family: "T1-D04"
priority: "required"
production_status: "planned"
release_gates: ["R04a"]
---

# DIA-04 — ByRef versus ByVal: caller variable and local parameter

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Visualize parameter passing without turning the script’s simplified copy metaphor into a false claim about arrays.

**Original plan family:** `T1-D04` — ByRef vs ByVal diagram

**Primary owner role:** motion. **Type:** motion. **Script section(s):** 7, 10. **Scope priority:** required.

**Method:** Offline HTML/CSS/inline SVG with deterministic JavaScript seeking; exported review MP4 and PNG stills.

**Owned write paths:** `assets/diagrams/DIA-04/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:19–19` ([original](../../sources/ASSET_PLAN.md))
- `SCRIPT.md:273–275` ([original](../../sources/SCRIPT.md))
- `SCRIPT.md:443–447` ([original](../../sources/SCRIPT.md))
- `Program.vb:149–152` ([original](../../sources/Program.vb))
- `Program.vb:204–208` ([original](../../sources/Program.vb))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Use an Integer-valued teaching example with caller variable x=7. Left panel ByRef: changing the parameter to 9 changes x. Right panel ByVal: the local parameter becomes 9 while x remains 7.
2. Do not draw a complete copied Deck or Hand array for ByVal. The supplied code mixes structures and array references; source-file semantics alone do not substantiate deep-copy claims.
3. Explicitly label the example Integer values. An array-reference extension requires the R04a evidence decision and its own explanatory state.
4. Historical default-passing statements stay out of the graphic until separately verified.

## Exact copy / source payload
~~~~text
Integer example
ByRef: caller variable can change
ByVal: local value changes; caller stays the same
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. 0–2 s: show two caller x=7 boxes and their parameters.
2. 2–6 s: resolve the ByRef connection and update to 9.
3. 6–10 s: resolve the ByVal local copy and update only the local parameter.
4. 10–12 s: hold both caller outcomes.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** See the specific requirements.

## Deliverables
Paths below are relative to `assets/diagrams/DIA-04/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `evidence/claim-checks.json`
- `src/index.html`
- `src/timeline.json`
- `exports/preview.mp4`
- `exports/poster.png`
- `exports/keyframes/start.png`
- `exports/keyframes/middle.png`
- `exports/keyframes/end.png`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] The two results are numerically unambiguous: caller 9 versus caller 7.
- [ ] No duplicated array storage is depicted as an automatic result of ByVal.
- [ ] ByRef and ByVal labels remain visible in every state.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
- **R04a** — read the corresponding entry in [EDITORIAL_REGISTER.md](../EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.

## Ticket-specific notes
The concrete Integer example is a proposed teaching implementation, not an additional supplied code listing. Verify its VB.NET behavior in a separate harness before release.

## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:19–19

~~~~text
| 4 | ByRef vs ByVal diagram | SVG — two-panel reference vs copy arrows |
~~~~
### SCRIPT.md:273–275

~~~~text
Second: `ByRef`. See that in the parameter list? In VB, you had to explicitly say whether a parameter was passed by reference or by value. `ByRef` means the subroutine receives a pointer to the original — changes are visible to the caller. `ByVal` means it gets a copy. In classic VB4, the default was `ByRef` for everything, which was efficient but dangerous. It meant you could accidentally modify data the caller didn't expect you to touch.

**[VISUAL: Diagram — ByRef: arrow pointing from sub back to caller's variable. ByVal: a copy being made, arrow pointing to a separate box.]**
~~~~
### SCRIPT.md:443–447

~~~~text
Sub PlayRound(ByVal RoundNumber As Integer, ByRef Player1 As Hand, _
              ByRef Player2 As Hand, ByRef WarCount As Integer)
```

Four parameters. The round number is passed `ByVal` because we just read it. The two hands and the war counter are `ByRef` because this subroutine modifies them.
~~~~
### Program.vb:149–152

~~~~text
    Sub AddCardToBottom(ByRef H As Hand, ByVal C As Card)
        H.Cards(H.Count) = C
        H.Count = H.Count + 1
    End Sub
~~~~
### Program.vb:204–208

~~~~text
    Sub PlayRound(ByVal RoundNumber As Integer, ByRef Player1 As Hand, _
                  ByRef Player2 As Hand, ByRef WarCount As Integer)

        Dim Pot(HAND_CAPACITY * 2 - 1) As Card
        Dim PotCount As Integer
~~~~

## Production delivery — this batch

**Media/source production: produced. Publication status: blocked.**

[Individual delivery inventory](../../assets/diagrams/DIA-04/delivery.json) · [QA and remaining decisions](../../assets/diagrams/DIA-04/qa.md) · [Live state](../../assets/diagrams/DIA-04/state.json)

The work order above remains unchanged. See the package gallery for rendered previews. Production does not clear an unresolved historical or editorial gate.
