---
id: "OPS-02"
title: "Isolated .NET harness and capture preflight"
owner_role: "capture"
original_family: "COORDINATION"
priority: "required"
production_status: "planned"
release_gates: ["R17"]
---

# OPS-02 — Isolated .NET harness and capture preflight

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Prepare a reliable local runner for authentic screenshots without modifying the supplied game.

**Original plan family:** `COORDINATION` — Build-pack coordination, not an original asset-plan media row.

**Primary owner role:** capture. **Type:** support. **Script section(s):** 4, 11. **Scope priority:** required.

**Method:** Scoped coordination/infrastructure work; exact owned paths and evidence report. No implicit asset production.

**Owned write paths:** `assets/ops/OPS-02/`, `tools/war-harness/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `Program.vb:1–29` ([original](../../sources/Program.vb))
- `SCRIPT.md:144–166` ([original](../../sources/SCRIPT.md))

**Integration dependencies:** None. This ticket may begin after the coordinator assigns it.

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Inspect whether a .NET SDK capable of VB console builds is already installed; record dotnet --info and --version. Do not assume a version or silently install system software.
2. Create tools/war-harness from dotnet new console -lang VB with an actual War.vbproj, copy sources/Program.vb byte-for-byte, and record hashes.
3. If explicit project OptionExplicit/OptionStrict nodes are needed for XTRA-06, add them only in the derived harness, record the diff, and preserve the generated target framework.
4. Build and execute once in isolation, retaining stdout/stderr/exit codes. No code bug is silently fixed for the video.
5. Publish an immutable harness snapshot for capture workers to copy into per-asset workspaces, preventing parallel bin/obj write races.

## Exact copy / source payload
Use the actual acquired/captured content described below. **No substitute screenshot text, invented runtime numbers, source URLs, or provenance is supplied.**

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. Inspect SDK and safe working directory.
2. Scaffold, copy source, record derived project-file decisions.
3. Build and run once; record actual behavior.
4. Freeze snapshot and capture instructions, or return a specific capability blocker.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `harness`

## Deliverables
Paths below are relative to `assets/ops/OPS-02/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `evidence/claim-checks.json`
- `exports/report.md`
- `evidence/commands.log`
- `exports/harness-manifest.json`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] Source hash remains identical to the uploaded Program.vb.
- [ ] No .vbproj is falsely identified as an uploaded original.
- [ ] The observed build result is recorded, including failures.
- [ ] Workers can make independent copies and run without shared output-directory contention.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
- **R17** — read the corresponding entry in [EDITORIAL_REGISTER.md](../EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### Program.vb:1–29

~~~~text
' War.vb
' A simulation of the card game War between two computer players.
' Written in the style of classic Visual Basic (VB4/VB5, circa 1995-96):
' one Module, one Sub Main, plain procedural flow, no LINQ, no lambdas,
' User-Defined Types (Structures) and parallel logic instead of classes.

Option Explicit On
Option Strict On

Module Program

    ' A single playing card. Rank runs 2 through 14 (11=Jack, 12=Queen,
    ' 13=King, 14=Ace). Suit is kept only for the printout - War does not
    ' care about suits.
    Structure Card
        Dim Rank As Integer
        Dim Suit As Char
    End Structure

    ' A player's hand of cards, stored as a simple array acting as a queue.
    ' Cards(0) is the top of the deck (next card to be played).
    ' Count is how many of the array slots are actually in use.
    Structure Hand
        Dim Cards() As Card
        Dim Count As Integer
    End Structure

    Const HAND_CAPACITY As Integer = 52
    Const MAX_ROUNDS As Integer = 20000
~~~~
### SCRIPT.md:144–166

~~~~text
**[VISUAL: Modern terminal — `dotnet new console -lang VB` being typed and executed.]**

**NARRATION:**

Here's a small confession: you cannot legally install Visual Basic 4.0 in 2024. Microsoft stopped selling it decades ago. There's no download. The license doesn't transfer. You could find it on abandonware sites, but I'm making a YouTube video, so let's stay on the right side of copyright law.

What you can do is use VB.NET — the modern descendant of Visual Basic, which ships with every copy of the .NET SDK. When you install .NET, you get a compiler called `vbc` — the Visual Basic compiler. It's the real thing. Microsoft still maintains it.

**[VISUAL: Terminal showing `dotnet --version` returning a version number, then the project creation command.]**

So we create a new console project:

```
dotnet new console -lang VB
```

And we get a `.vbproj` file and a `Program.vb` file. That's our canvas.

Now, the code I'm going to write uses modern VB.NET syntax, but I'm deliberately writing it in the old style. One module. One `Sub Main`. Structures instead of classes. Manual array management instead of generic collections. `Select Case` instead of pattern matching. `Do While...Loop` instead of LINQ. If you squint at this code, it should feel like something you'd see in a VB4 textbook from 1996.

**[VISUAL: The War.vbproj file, briefly, with `OptionExplicit` and `OptionStrict` highlighted.]**

Two important settings in the project file: `Option Explicit On` and `Option Strict On`. In classic VB, variables didn't need to be declared. You could just start using a variable called `x` and VB would create it for you on the spot as a Variant — a type that could hold anything. This was convenient and also the source of approximately ten million bugs, because if you misspelled a variable name, VB would just silently create a new empty variable instead of telling you something was wrong. `Option Explicit` forces you to declare everything. We're being responsible.
~~~~
