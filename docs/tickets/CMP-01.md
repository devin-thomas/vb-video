---
id: "CMP-01"
title: "Variable declaration: type before versus after"
owner_role: "code"
original_family: "T1-S04"
priority: "required"
production_status: "planned"
release_gates: []
---

# CMP-01 — Variable declaration: type before versus after

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Build an independently reusable two-column C# / VB syntax comparison with matched visual emphasis.

**Original plan family:** `T1-S04` — Side-by-side C# vs VB comparisons (4+)

**Primary owner role:** code. **Type:** still. **Script section(s):** 6. **Scope priority:** required.

**Method:** Editable SVG for diagrams; offline HTML/CSS for composed text/image layouts. Export PNG poster and named state variants.

**Owned write paths:** `assets/comparisons/CMP-01/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:38–38` ([original](../../sources/ASSET_PLAN.md))
- `SCRIPT.md:223–229` ([original](../../sources/SCRIPT.md))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Names and capitalization follow the script; emphasize that VB puts the type after As. Avoid implying different numeric ranges are being taught here.
2. Left column C#, right column VB. Use equal area and the same code font size; do not make either language artificially harder to read.
3. Retain literal supplied examples where present. Proposed counterparts must be recorded as agent-authored explanatory snippets and checked in an isolated context, not committed to Program.vb.
4. Ellipses mean an intentional excerpt, never runnable complete source. Only the compared tokens receive accent highlighting.

## Exact copy / source payload
~~~~text
C#
int rank;
char suit;

VB
Dim Rank As Integer
Dim Suit As Char
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. Still: both code columns visible at once.
2. Focus overlay: highlight the specific contrasting tokens without changing layout.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `comparison`, `token-focus`

## Deliverables
Paths below are relative to `assets/comparisons/CMP-01/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `src/index.html`
- `exports/poster.png`
- `exports/comparison.png`
- `exports/token-focus.png`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] Both column headings and the exact comparison subject are visible.
- [ ] Comparable examples use matching loop bounds/data where applicable.
- [ ] The code does not make a stronger semantic-equivalence claim than the source supports.
- [ ] A 720p proof has legible punctuation and no clipped code.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
No specific editorial register item is assigned. New outside claims or third-party materials still require evidence and rights review.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:38–38

~~~~text
| 4 | Side-by-side C# vs VB comparisons (4+) | HTML two-column layout |
~~~~
### SCRIPT.md:223–229

~~~~text
Now, let's talk about something that might look weird if you're coming from C# or C++: the keyword `Dim`.

**[VISUAL: The word `Dim` highlighted, with a thought bubble: "Dimension?"]**

`Dim` is how you declare a variable in Visual Basic. It stands for "Dimension" — as in, dimension an array, allocate space for this thing. That name goes all the way back to the original 1964 BASIC at Dartmouth. In the earliest versions, `DIM` was specifically for declaring the dimensions of arrays. Over time, it got repurposed to declare any variable. Fifty years later, VB programmers are still typing `Dim` and most of them have no idea they're invoking an abbreviation from 1964.

Compare that to C#, where you'd write `int rank;` or `char suit;`. In VB, the type comes after the name: `Dim Rank As Integer`. It reads more like English: "Dimension a variable called Rank, as an Integer." That's very much a deliberate design choice — BASIC was always supposed to read like English.
~~~~

## Production delivery — this batch

**Media/source production: produced. Publication status: unreviewed.**

[Individual delivery inventory](../../assets/comparisons/CMP-01/delivery.json) · [QA and remaining decisions](../../assets/comparisons/CMP-01/qa.md) · [Live state](../../assets/comparisons/CMP-01/state.json)

The work order above remains unchanged. See the package gallery for rendered previews. Production does not clear an unresolved historical or editorial gate.
