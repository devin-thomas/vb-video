---
id: "XTRA-19"
title: "Period Windows FreeCell screenshot"
owner_role: "research"
original_family: "SCRIPT-SUPPLEMENT"
priority: "optional-script"
production_status: "planned"
release_gates: ["R14"]
---

# XTRA-19 — Period Windows FreeCell screenshot

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Fill this named script visual omitted from the categorized asset plan, with a separately reviewable source package.

**Original plan family:** `SCRIPT-SUPPLEMENT` — A visual explicitly requested in SCRIPT.md but omitted from the categorized plan.

**Primary owner role:** research. **Type:** archive. **Script section(s):** 12. **Scope priority:** optional-script.

**Method:** Browser/archive discovery plus native original acquisition or uncropped screenshot; reversible framing and evidence files.

**Owned write paths:** `assets/historical/XTRA-19/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `SCRIPT.md:579–593` ([original](../../sources/SCRIPT.md))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Find a distinct Windows FreeCell screenshot appropriate to the montage. Confirm the version/context rather than substituting a modern remake. This is period UI reference, not proof of VB authorship.
2. Locate and inspect the underlying archive/publication/creator page, not only a search thumbnail. Record source URL, content date, access date, attribution, permitted uses, and identity evidence.
3. Keep an original or uncropped capture and a separate 16:9 editorial framing copy. Do not stretch screenshots or crop away contrary version/date evidence.
4. Public availability and “educational use” are not a license record. If permission is unclear, retain the candidate as release-blocked and make a specific human handoff.
5. If no adequate source exists, return a documented no-match result and candidate limitations; never fill the gap with invented archival material.

## Exact copy / source payload
Use the actual acquired/captured content described below. **No substitute screenshot text, invented runtime numbers, source URLs, or provenance is supplied.**

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. Find and verify this single source subject.
2. Record provenance and rights.
3. Acquire through a permitted route or record the access block.
4. Prepare the reversible editorial frame and individual credit record.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `original`, `editorial-frame`

## Deliverables
Paths below are relative to `assets/historical/XTRA-19/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `evidence/claim-checks.json`
- `source/original.*`
- `evidence/source.json`
- `evidence/rights.json`
- `exports/editorial-frame.png`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] The image depicts the specifically named subject and era, with evidence.
- [ ] Attribution/rights status is recorded without guessing.
- [ ] Raw and presentation copies are distinguishable.
- [ ] This script-only addition is not mislabeled as an original asset-plan row.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
- **R14** — read the corresponding entry in [EDITORIAL_REGISTER.md](../EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### SCRIPT.md:579–593

~~~~text
**[VISUAL: Screenshots of 90s shareware card games — Solitaire, Hearts, FreeCell. Windows 3.1 and Windows 95 versions. The classic green felt background.]**

**NARRATION:**

Okay, so we've built a console War simulator. In 1995, what would have happened next?

You would have put a GUI on it.

This is where Visual Basic's form designer would have come in. You'd open the IDE, draw a form the size of a card table, drop some `PictureBox` controls on it to represent the card positions, maybe load bitmap images of playing cards from a resource file, and wire up the game logic to a Timer control that advanced the game one round at a time. You'd add a "Deal" button, a "Watch" button, maybe a speed slider.

**[VISUAL: Mockup of what a VB4 War GUI might look like — a green-felt form with card images, buttons along the bottom. Labeled: "This is what you'd build next."]**

And this is how a lot of actual software got made in the mid-90s. People would start with the logic — get the algorithms working in a console or a simple form — and then layer the visual design on top. Visual Basic made that second step dramatically easier than anything else available.

The shareware scene of the 1990s was full of card games, board games, and puzzle games written in Visual Basic. Windows came bundled with Solitaire and Minesweeper (written in C), but the thousands of freeware and shareware alternatives that flooded BBS boards and early web sites? A huge number of those were VB apps.
~~~~
