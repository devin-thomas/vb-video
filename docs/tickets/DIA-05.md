---
id: "DIA-05"
title: "Array as queue: live slots, draw, shift, append"
owner_role: "motion"
original_family: "T1-D05"
priority: "required"
production_status: "planned"
release_gates: []
---

# DIA-05 — Array as queue: live slots, draw, shift, append

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Explain the exact manual queue represented by Hand.Cards and Hand.Count.

**Original plan family:** `T1-D05` — Array-as-queue animation

**Primary owner role:** motion. **Type:** motion. **Script section(s):** 8. **Scope priority:** required.

**Method:** Offline HTML/CSS/inline SVG with deterministic JavaScript seeking; exported review MP4 and PNG stills.

**Owned write paths:** `assets/diagrams/DIA-05/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:20–20` ([original](../../sources/ASSET_PLAN.md))
- `SCRIPT.md:315–360` ([original](../../sources/SCRIPT.md))
- `Program.vb:23–26` ([original](../../sources/Program.vb))
- `Program.vb:128–167` ([original](../../sources/Program.vb))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Include an overview of all 52 slots, then a readable zoom of the first slots. Slots 0 through Count−1 are live; slots at or after Count are inactive, not necessarily zeroed.
2. Use Count=4 and illustrative cards [2♠,5♥,9♦,K♣]. Draw 2♠, copy 5♥→0,9♦→1,K♣→2, decrement Count to 3. The old last slot may remain stale.
3. Append A♠ at index Count=3 and then increment Count to 4. Preserve first-in/first-out semantics.
4. Count is an integer marking the live boundary, not a memory-address pointer. Keep “next to draw” at index 0.

## Exact copy / source payload
~~~~text
Capacity: 52
Cards(0): next to draw
Live: 0 … Count − 1
Inactive: Count … 51
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. 0–3 s: 52-slot overview, capacity 52, live boundary at 4.
2. 3–6 s: zoom and take index 0 into the returned-card area.
3. 6–11 s: perform the three assignments in increasing i order.
4. 11–14 s: Count becomes 3 and the stale tail becomes inactive.
5. 14–18 s: append A♠ at index 3; Count becomes 4.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `52-slot-overview`, `draw-and-shift`, `append-at-count`

## Deliverables
Paths below are relative to `assets/diagrams/DIA-05/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `src/index.html`
- `src/timeline.json`
- `exports/preview.mp4`
- `exports/poster.png`
- `exports/keyframes/start.png`
- `exports/keyframes/middle.png`
- `exports/keyframes/end.png`
- `exports/52-slot-overview.png`
- `exports/draw-and-shift.png`
- `exports/append-at-count.png`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] No card is drawn from the array end.
- [ ] An inactive tail slot is not presented as an extra live card.
- [ ] Shift uses H.Cards(i - 1) = H.Cards(i), and the Count decrement happens afterward.
- [ ] Both overview and draw/append segments have exportable hold frames.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
No specific editorial register item is assigned. New outside claims or third-party materials still require evidence and rights review.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:20–20

~~~~text
| 5 | Array-as-queue animation | HTML with step-through JS or multi-frame SVG |
~~~~
### SCRIPT.md:315–360

~~~~text
Here's where things get interesting from a data structures perspective. Each player has a hand of cards, and that hand needs to behave like a queue — first in, first out. When you win cards, they go to the bottom of your pile. When you play a card, it comes off the top.

In C#, you'd use `Queue<Card>` and call it a day. In C++, you'd use `std::queue`. In 1995 VB? You didn't have generic collections. You didn't even have a built-in queue. You had arrays, and you had `ReDim`, and that was about it.

So we fake it:

```vb
Structure Hand
    Dim Cards() As Card
    Dim Count As Integer
End Structure
```

**[VISUAL: A visual diagram of the Hand structure — an array of 52 slots, with only the first N filled. An arrow labeled "Count" points to the boundary.]**

The hand is an array of 52 slots — the maximum possible, since you could theoretically hold all the cards — and a counter that tracks how many slots are actually in use. Cards zero through Count-minus-one are live. Everything past Count is garbage.

Adding a card to the bottom is simple — drop it at position Count and increment:

```vb
Sub AddCardToBottom(ByRef H As Hand, ByVal C As Card)
    H.Cards(H.Count) = C
    H.Count = H.Count + 1
End Sub
```

Drawing from the top is more expensive. You take the card at position zero, then shift everything else forward by one slot:

```vb
Function DrawTopCard(ByRef H As Hand) As Card
    Dim Top As Card
    Dim i As Integer

    Top = H.Cards(0)
    For i = 1 To H.Count - 1
        H.Cards(i - 1) = H.Cards(i)
    Next i
    H.Count = H.Count - 1

    DrawTopCard = Top
End Function
```

**[VISUAL: Animation — cards in a row. The leftmost card is removed, and all the others slide left to fill the gap.]**

That shift is O(n) — every card in the hand moves one position. With a proper circular buffer or a linked list, you could do this in O(1). But we're writing 1995 VB. Nobody was thinking about algorithmic complexity when they had 26 cards in a hand. Computers were slower, but data sets were tiny. The whole game finishes in a few hundred rounds, and each round shifts at most 52 cards. A Pentium 75 could handle this without breaking a sweat.
~~~~
### Program.vb:23–26

~~~~text
    Structure Hand
        Dim Cards() As Card
        Dim Count As Integer
    End Structure
~~~~
### Program.vb:128–167

~~~~text
    Function NewHand() As Hand
        Dim H As Hand
        ReDim H.Cards(HAND_CAPACITY - 1)
        H.Count = 0
        NewHand = H
    End Function

    ' Deals the 52-card deck alternately into the two hands, 26 apiece.
    Sub DealCards(ByRef Deck() As Card, ByRef Player1 As Hand, ByRef Player2 As Hand)
        Dim i As Integer

        For i = 0 To 51
            If i Mod 2 = 0 Then
                AddCardToBottom(Player1, Deck(i))
            Else
                AddCardToBottom(Player2, Deck(i))
            End If
        Next i
    End Sub

    ' Adds a card to the bottom (end) of a hand's queue.
    Sub AddCardToBottom(ByRef H As Hand, ByVal C As Card)
        H.Cards(H.Count) = C
        H.Count = H.Count + 1
    End Sub

    ' Removes and returns the top card of a hand's queue, shifting the
    ' remaining cards up by one slot.
    Function DrawTopCard(ByRef H As Hand) As Card
        Dim Top As Card
        Dim i As Integer

        Top = H.Cards(0)
        For i = 1 To H.Count - 1
            H.Cards(i - 1) = H.Cards(i)
        Next i
        H.Count = H.Count - 1

        DrawTopCard = Top
    End Function
~~~~

## Production delivery — this batch

**Media/source production: produced. Publication status: unreviewed.**

[Individual delivery inventory](../../assets/diagrams/DIA-05/delivery.json) · [QA and remaining decisions](../../assets/diagrams/DIA-05/qa.md) · [Live state](../../assets/diagrams/DIA-05/state.json)

The work order above remains unchanged. See the package gallery for rendered previews. Production does not clear an unresolved historical or editorial gate.
