---
id: "DIA-06"
title: "Fisher\u2013Yates shuffle: backward swaps"
owner_role: "motion"
original_family: "T1-D06"
priority: "required"
production_status: "planned"
release_gates: ["R15"]
---

# DIA-06 — Fisher–Yates shuffle: backward swaps

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Show why the shrinking swap range matters, using a small readable array and the exact bounds in the real code.

**Original plan family:** `T1-D06` — Fisher-Yates shuffle animation

**Primary owner role:** motion. **Type:** motion. **Script section(s):** 7. **Scope priority:** required.

**Method:** Offline HTML/CSS/inline SVG with deterministic JavaScript seeking; exported review MP4 and PNG stills.

**Owned write paths:** `assets/diagrams/DIA-06/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:21–21` ([original](../../sources/ASSET_PLAN.md))
- `SCRIPT.md:283–305` ([original](../../sources/SCRIPT.md))
- `Program.vb:114–126` ([original](../../sources/Program.vb))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Use tools/fixtures/shuffle_storyboard.json: eight illustrative cards, indices 0–7; make the reduced-size example explicit.
2. At each step highlight i, the allowed j range 0…i, the selected j, and the temporary Card used during the swap.
3. Include a self-swap, which is allowed. After each iteration, the fixed suffix grows; never choose j above i.
4. End with the real-code bounds i=51 down to 1 and Rnd.Next(0, i + 1), stating the latter as source code rather than hiding the +1.
5. Do not use this deterministic teaching trace as statistical evidence of unbiasedness or invent benchmark claims.

## Exact copy / source payload
~~~~text
Small example: 8 cards
Choose j from 0 through i
Swap Deck(i) and Deck(j)
Real deck: i = 51 down to 1
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. 0–3 s: label the eight-card illustration and indices.
2. 3–15 s: execute all seven stored swaps, with extra hold on first swap and self-swap.
3. 15–18 s: show final permutation and locked suffix.
4. 18–21 s: connect the miniature to the 52-card code.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `miniature-trace`, `real-code-bounds`

## Deliverables
Paths below are relative to `assets/diagrams/DIA-06/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `evidence/claim-checks.json`
- `src/index.html`
- `src/timeline.json`
- `exports/preview.mp4`
- `exports/poster.png`
- `exports/keyframes/start.png`
- `exports/keyframes/middle.png`
- `exports/keyframes/end.png`
- `exports/miniature-trace.png`
- `exports/real-code-bounds.png`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] All fixture steps are followed and final order matches the fixture.
- [ ] Cards are permuted, never duplicated or deleted.
- [ ] Every j is inside 0…i and a fixed suffix card is not swapped later.
- [ ] A historical “1938” caption is excluded pending verification.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
- **R15** — read the corresponding entry in [EDITORIAL_REGISTER.md](../EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:21–21

~~~~text
| 6 | Fisher-Yates shuffle animation | HTML with step-through JS or multi-frame SVG |
~~~~
### SCRIPT.md:283–305

~~~~text
```vb
Sub ShuffleDeck(ByRef Deck() As Card)
    Dim Rnd As New Random()
    Dim i As Integer
    Dim j As Integer
    Dim Temp As Card

    For i = 51 To 1 Step -1
        j = Rnd.Next(0, i + 1)
        Temp = Deck(i)
        Deck(i) = Deck(j)
        Deck(j) = Temp
    Next i
End Sub
```

This is a Fisher-Yates shuffle — the gold standard for producing an unbiased random permutation. You walk backwards through the array, and for each position, you swap it with a randomly chosen position at or before it. Every possible ordering of the deck is equally likely. This is the same algorithm you'd use in C++ or C# or Python or anything else. It's one of those algorithms that's just correct, and has been since 1938 when Fisher and Yates published it.

**[VISUAL: Animation of the shuffle — an array of numbered cards, each step highlighting the swap. The array goes from ordered to shuffled.]**

The VB-specific thing to notice here is `For i = 51 To 1 Step -1`. That `Step -1` is how you count backwards. C-family languages just use `i--` in the for-loop update. VB makes you spell it out: "step by negative one." Verbose, but unambiguous.

Also notice: every single variable is declared at the top of the subroutine with `Dim`. In classic VB, you couldn't declare variables in the middle of the code. Everything went at the top. It's like writing C89 — all declarations before any statements. C# and modern C++ let you declare variables wherever you want. VB was stricter about this.
~~~~
### Program.vb:114–126

~~~~text
    Sub ShuffleDeck(ByRef Deck() As Card)
        Dim Rnd As New Random()
        Dim i As Integer
        Dim j As Integer
        Dim Temp As Card

        For i = 51 To 1 Step -1
            j = Rnd.Next(0, i + 1)
            Temp = Deck(i)
            Deck(i) = Deck(j)
            Deck(j) = Temp
        Next i
    End Sub
~~~~

## Production delivery — this batch

**Media/source production: produced. Publication status: blocked.**

[Individual delivery inventory](../../assets/diagrams/DIA-06/delivery.json) · [QA and remaining decisions](../../assets/diagrams/DIA-06/qa.md) · [Live state](../../assets/diagrams/DIA-06/state.json)

The work order above remains unchanged. See the package gallery for rendered previews. Production does not clear an unresolved historical or editorial gate.
