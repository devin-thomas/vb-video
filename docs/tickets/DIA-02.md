---
id: "DIA-02"
title: "War rules: shuffle, deal, flip, compare, collect"
owner_role: "motion"
original_family: "T1-D02"
priority: "required"
production_status: "planned"
release_gates: ["R05"]
---

# DIA-02 — War rules: shuffle, deal, flip, compare, collect

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Create the reusable rules sequence for viewers who do not know War, including isolated deal and normal-round cutdowns.

**Original plan family:** `T1-D02` — War game rules animation

**Primary owner role:** motion. **Type:** motion. **Script section(s):** 5, 9. **Scope priority:** required.

**Method:** Offline HTML/CSS/inline SVG with deterministic JavaScript seeking; exported review MP4 and PNG stills.

**Owned write paths:** `assets/diagrams/DIA-02/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:17–17` ([original](../../sources/ASSET_PLAN.md))
- `SCRIPT.md:170–194` ([original](../../sources/SCRIPT.md))
- `SCRIPT.md:399–401` ([original](../../sources/SCRIPT.md))
- `Program.vb:136–152` ([original](../../sources/Program.vb))
- `Program.vb:234–268` ([original](../../sources/Program.vb))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Start from one 52-card deck and deal alternately, never by simply assigning the first 26 cards to one player in a code-specific view.
2. Keep Player 1 left, Player 2 right, pot center; counters sum to 52 after every transfer.
3. Teach Ace high, rank-only comparison, winning cards appended to the bottom, and the normal three-down/one-up war.
4. Use tools/fixtures/war_storyboard.json for illustrative states; these are teaching examples, not an alleged captured game.
5. Do not animate the disputed insufficient-cards ending until R05 is resolved; an ordinary adequately stocked war is safe.
6. Include the stated victory condition: win all 52 cards. Show it as a rule caption in the closing hold, not as a fabricated completion of the miniature example. The insufficient-cards rule remains scoped to R05.

## Exact copy / source payload
~~~~text
52 cards · 26 each
Higher rank wins · Aces high
Tie? War.
Won cards go to the bottom
Win all 52 cards.
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. 0–4 s: full deck, shuffle abstraction, 52 label.
2. 4–10 s: alternating deal ends at 26 / 26 / pot 0.
3. 10–16 s: A♠ beats K♥; pot 2 travels to Player 1 bottom; counters become 27 / 25.
4. 16–24 s: reset visibly to the independent single-war fixture and show the tie, six burns, and deciding pair.
5. 24–28 s: collect the ten-card pot and hold the rank-only rule.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `full-rules`, `alternating-deal`, `normal-round`, `single-war`

## Deliverables
Paths below are relative to `assets/diagrams/DIA-02/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `evidence/claim-checks.json`
- `src/index.html`
- `src/timeline.json`
- `exports/preview.mp4`
- `exports/poster.png`
- `exports/keyframes/start.png`
- `exports/keyframes/middle.png`
- `exports/keyframes/end.png`
- `exports/full-rules.png`
- `exports/alternating-deal.png`
- `exports/normal-round.png`
- `exports/single-war.png`
- `exports/full-rules.mp4`
- `exports/alternating-deal.mp4`
- `exports/normal-round.mp4`
- `exports/single-war.mp4`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] Every visible card has a unique identity within its scene.
- [ ] Deal cutdown independently opens and closes on readable states.
- [ ] No strategic choices, suit trumping, or additional randomness during play are implied.
- [ ] Normal round has 2 cards, ordinary war has 10, and awarded cards enter the queue bottom.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
- **R05** — read the corresponding entry in [EDITORIAL_REGISTER.md](../EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:17–17

~~~~text
| 2 | War game rules animation | HTML with CSS animations or multi-frame SVG |
~~~~
### SCRIPT.md:170–194

~~~~text
## SECTION 5: THE CARD GAME WAR — RULES IN 60 SECONDS

**[VISUAL: Animated diagram of a War game — two decks, cards flipping, a "pot" in the middle. Simple, clean, maybe hand-drawn style.]**

**NARRATION:**

Before we write any code, let's make sure we all know how War works, because you might not have played it since you were eight. Or maybe you've never played it at all. It's a children's card game — no strategy, no decisions, pure luck — which makes it perfect for a computer simulation, because there are no choices to model.

Here are the rules:

One. Take a standard 52-card deck and shuffle it. Deal it evenly — 26 cards to each player, face down. Neither player looks at their cards.

**[VISUAL: Animation — deck splits into two piles.]**

Two. Each round, both players flip their top card face-up at the same time. Whoever played the higher card takes both cards and puts them at the bottom of their pile. Aces are high — they beat everything.

**[VISUAL: Animation — two cards flip, the higher one "wins" and both slide to the bottom of a pile.]**

Three. If both players flip the same rank — two sevens, two kings, whatever — it's War. Each player places three cards face-down, then flips a fourth card face-up. Whoever's face-up card is higher takes the entire pot — all ten cards. If it's another tie, you do it again. War can chain.

**[VISUAL: Animation — two equal cards appear, then three face-down cards from each player, then two more face-up cards. The pot grows.]**

Four. You keep playing until one player has all 52 cards. That player wins. If a player can't put up enough cards for a war, they lose.

That's it. No bluffing, no bidding, no trump suits. Just flip, compare, collect. A game a five-year-old can play — and a game that maps beautifully onto about 250 lines of code.
~~~~
### SCRIPT.md:399–401

~~~~text
We build a deck, shuffle it, create two empty hands, and deal — alternating cards, one to Player 1, one to Player 2, until all 52 are distributed. Each player gets 26.

**[VISUAL: Animation — a deck splitting into two piles, cards alternating left and right.]**
~~~~
### Program.vb:136–152

~~~~text
    Sub DealCards(ByRef Deck() As Card, ByRef Player1 As Hand, ByRef Player2 As Hand)
        Dim i As Integer

        For i = 0 To 51
            If i Mod 2 = 0 Then
                AddCardToBottom(Player1, Deck(i))
            Else
                AddCardToBottom(Player2, Deck(i))
            End If
        Next i
    End Sub

    ' Adds a card to the bottom (end) of a hand's queue.
    Sub AddCardToBottom(ByRef H As Hand, ByVal C As Card)
        H.Cards(H.Count) = C
        H.Count = H.Count + 1
    End Sub
~~~~
### Program.vb:234–268

~~~~text
            Card1 = DrawTopCard(Player1)
            Card2 = DrawTopCard(Player2)
            Pot(PotCount) = Card1 : PotCount = PotCount + 1
            Pot(PotCount) = Card2 : PotCount = PotCount + 1

            If WarNumber = 0 Then
                Console.WriteLine("Player 1 plays " & CardText(Card1) & _
                    ", Player 2 plays " & CardText(Card2) & ".")
            Else
                Console.WriteLine("        War card - Player 1 plays " & CardText(Card1) & _
                    ", Player 2 plays " & CardText(Card2) & ".")
            End If

            If Card1.Rank > Card2.Rank Then
                Console.WriteLine("        Player 1 wins the round (" & PotCount & " cards).")
                GiveCardsToWinner(Pot, PotCount, Player1)
                Exit Sub

            ElseIf Card2.Rank > Card1.Rank Then
                Console.WriteLine("        Player 2 wins the round (" & PotCount & " cards).")
                GiveCardsToWinner(Pot, PotCount, Player2)
                Exit Sub

            Else
                ' Tie: it's WAR. Each side burns up to 3 face-down cards,
                ' then both flip a new card to compare.
                WarNumber = WarNumber + 1
                WarCount = WarCount + 1
                Console.WriteLine("        ** WAR! ** (war number " & WarNumber & " this round)")

                BurnCount = Math.Min(3, Math.Min(Player1.Count, Player2.Count))
                For i = 1 To BurnCount
                    Pot(PotCount) = DrawTopCard(Player1) : PotCount = PotCount + 1
                    Pot(PotCount) = DrawTopCard(Player2) : PotCount = PotCount + 1
                Next i
~~~~

## Production delivery — this batch

**Media/source production: produced. Publication status: blocked.**

[Individual delivery inventory](../../assets/diagrams/DIA-02/delivery.json) · [QA and remaining decisions](../../assets/diagrams/DIA-02/qa.md) · [Live state](../../assets/diagrams/DIA-02/state.json)

The work order above remains unchanged. See the package gallery for rendered previews. Production does not clear an unresolved historical or editorial gate.

## Revision 2 — 2026-09-16 (Devin's review of the first cut, note 9 and 11)

**Status:** production back to `planned`; release back to `unreviewed` until the producer approves the new exports. Work on branch `ticket/DIA-02-r2`; write only inside the ticket's owned paths. Keep every requirement above unless this section changes it.

The rules explanation must be beat-accurate. Re-cut (or re-render) the sequence into cutdowns that each start at a named moment and hold their end state for as long as the editor needs: `deck-hold` (full squared deck, no motion, loopable), `alternating-deal` (deal to 26/26, ends holding), `normal-round` (A♠ beats K♥, the pot travels, counters 27/25, ends holding), `single-war` (the tie, three face-down each, the deciding flip, the ten-card pot collected, ends holding), and `final-hold` (rank-only rule card). Each cutdown's delivery.json entry records the second at which its key moment occurs (the ace landing, the tie appearing, the deciding flip) so ASM-03 can place it on the word. Use the revised shared card faces (OPS-01) so the cards look like real cards. Anything not named by the narration stays out of the cutdowns.

**Origin:** [review/cut-notes-2026-09-16.md](../../review/cut-notes-2026-09-16.md). Report as for any ticket: ID, branch and commit, files, tests run, validator result, review questions.
