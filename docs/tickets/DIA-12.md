---
id: "DIA-12"
title: "Windows VB4 versus Mac CodeWarrior table"
owner_role: "design"
original_family: "T1-D12"
priority: "required"
production_status: "planned"
release_gates: ["R03", "R09", "R16"]
---

# DIA-12 — Windows VB4 versus Mac CodeWarrior table

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Lay out the exact five-row comparison from the script and make disputed wording visible to the editorial process before final export.

**Original plan family:** `T1-D12` — Mac vs Windows comparison table

**Primary owner role:** design. **Type:** still. **Script section(s):** 14. **Scope priority:** required.

**Method:** Editable SVG for diagrams; offline HTML/CSS for composed text/image layouts. Export PNG poster and named state variants.

**Owned write paths:** `assets/diagrams/DIA-12/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:27–27` ([original](../../sources/ASSET_PLAN.md))
- `SCRIPT.md:659–671` ([original](../../sources/SCRIPT.md))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Preserve the supplied draft cells in data: Language BASIC / C-C++; GUI builder Yes (drag & drop) / Sort of (ResEdit); Learning curve Low / Medium-high; Runtime required Yes (VB runtime DLL) / No (native binary); Console app like ours Trivial / Also pretty easy, actually.
2. Use Windows (VB4) and Mac (CodeWarrior) column headings. No unverified product screenshot is needed.
3. The table is draft source copy, not verified truth. Console support, the ResEdit characterization, dates, and runtime claims require R03/R09/R16 decisions.
4. Keep neutral styling; do not reinforce the comparison using invented benchmark bars.

## Exact copy / source payload
~~~~text
Windows (VB4) | Mac (CodeWarrior)
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. Static three-column table: row label, Windows, Mac.
2. Optional focus state dims all but the GUI-builder row.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `full-table`, `gui-row-focus`

## Deliverables
Paths below are relative to `assets/diagrams/DIA-12/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `evidence/claim-checks.json`
- `src/scene.svg`
- `exports/poster.png`
- `exports/full-table.png`
- `exports/gui-row-focus.png`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] Five source rows and both platforms are included.
- [ ] Every changed cell has an explicit approved editorial decision; otherwise proof remains release-blocked.
- [ ] The comparison does not present modern VB.NET console output as a literal classic-VB4 environment.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
- **R03** — read the corresponding entry in [EDITORIAL_REGISTER.md](../EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.
- **R09** — read the corresponding entry in [EDITORIAL_REGISTER.md](../EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.
- **R16** — read the corresponding entry in [EDITORIAL_REGISTER.md](../EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:27–27

~~~~text
| 12 | Mac vs Windows comparison table | SVG or HTML table |
~~~~
### SCRIPT.md:659–671

~~~~text
**[VISUAL: Side-by-side comparison chart:]**

| | Windows (VB4) | Mac (CodeWarrior) |
|---|---|---|
| Language | BASIC | C/C++ |
| GUI builder | Yes (drag & drop) | Sort of (ResEdit) |
| Learning curve | Low | Medium-high |
| Runtime required | Yes (VB runtime DLL) | No (native binary) |
| Console app like ours | Trivial | Also pretty easy, actually |

For a console card game like ours — no GUI, just logic and text output — the difficulty would have been roughly comparable. C is a harder language than BASIC, but the actual logic of building a deck, shuffling, and comparing cards is the same in any language. You'd use structs, arrays, and printf instead of structures, arrays, and Console.WriteLine. The algorithm doesn't change.

Where VB had the massive advantage was in the next step — putting a GUI on it. In VB, that was an afternoon of dragging controls onto a form. On the Mac with CodeWarrior, you'd be writing Carbon or Toolbox API calls, managing window records, handling update events, and doing your own drawing into grafPorts. It was not an afternoon. It was a week, minimum, for someone who knew what they were doing.
~~~~
