---
id: "DIA-01"
title: "Event-driven programming: action \u2192 handler \u2192 code"
owner_role: "motion"
original_family: "T1-D01"
priority: "required"
production_status: "planned"
release_gates: ["R03"]
---

# DIA-01 — Event-driven programming: action → handler → code

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Explain why the VB form designer changed the unit of work from constructing windows to responding to events.

**Original plan family:** `T1-D01` — Event-driven programming diagram

**Primary owner role:** motion. **Type:** motion. **Script section(s):** 3. **Scope priority:** required.

**Method:** Offline HTML/CSS/inline SVG with deterministic JavaScript seeking; exported review MP4 and PNG stills.

**Owned write paths:** `assets/diagrams/DIA-01/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:16–16` ([original](../../sources/ASSET_PLAN.md))
- `SCRIPT.md:130–138` ([original](../../sources/SCRIPT.md))
- `SCRIPT.md:98–108` ([original](../../sources/SCRIPT.md))

**Integration dependencies:** [OPS-01](OPS-01.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Three clearly labeled user actions: click, type, resize. Each connects to a distinct Sub-handler box, then to an observable UI response.
2. Use the supplied Command1_Click / MsgBox example only for the click path. Other event names must be verified or remain descriptive labels, not fabricated classic-VB identifiers.
3. Distinguish event order from a continuously executing game loop. This is a GUI concept illustration, not a claim that Program.vb contains forms.

## Exact copy / source payload
~~~~text
Event-Driven Programming
Click · Type · Resize
Action → Handler → Response
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. 0–2 s: show a gray form and the three action labels.
2. 2–6 s: activate Click → Command1_Click → message box.
3. 6–10 s: demonstrate Type and Resize one at a time using descriptive handler labels.
4. 10–12 s: hold the whole action → handler → response system.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `overview`, `click-handler`

## Deliverables
Paths below are relative to `assets/diagrams/DIA-01/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `evidence/claim-checks.json`
- `src/index.html`
- `src/timeline.json`
- `exports/preview.mp4`
- `exports/poster.png`
- `exports/keyframes/start.png`
- `exports/keyframes/middle.png`
- `exports/keyframes/end.png`
- `exports/overview.png`
- `exports/click-handler.png`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] Every action reaches one handler and one result; arrows have visible direction.
- [ ] The code displayed for Command1_Click matches SCRIPT.md verbatim.
- [ ] The final hold explains the relationship without relying on motion or narration.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
- **R03** — read the corresponding entry in [EDITORIAL_REGISTER.md](../EDITORIAL_REGISTER.md). Keep the proof out of the cleared-media bin until its applicable decision is recorded.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:16–16

~~~~text
| 1 | Event-driven programming diagram | SVG — boxes and arrows |
~~~~
### SCRIPT.md:130–138

~~~~text
**[VISUAL: Side-by-side — the VB4 IDE with labeled callouts: Form Designer, Toolbox, Properties Window, Code Window, Project Explorer.]**

This IDE had a few key pieces. On the left, you had a toolbox — a vertical strip of icons for things like buttons, text boxes, labels, picture boxes, timers, scroll bars. In the center was your form — a blank gray window that you designed visually. On the right was the properties panel, where you could set things like the caption on a button or the font size of a label. And behind all of it was the code window, where you wrote your actual BASIC.

This was called "RAD" — Rapid Application Development. The whole point was speed. You weren't writing code to create a window. The IDE did that for you. You were writing code to respond to events — a button click, a timer tick, a key press.

**[VISUAL: Animated diagram — "Event-Driven Programming" — arrows from user actions (click, type, resize) pointing to Sub procedures in code.]**

Now, for today's project, we're not going to use any of that visual stuff. War is a console program — two CPUs playing cards against each other in a terminal. No forms, no buttons. But I want you to understand that the form designer was the reason people bought VB. The language itself was just one part of the package. The drag-and-drop GUI builder was the killer feature.
~~~~
### SCRIPT.md:98–108

~~~~text
Here's what made Visual Basic revolutionary: in C, if you wanted a button that said "Click Me" and popped up a message, you were writing dozens of lines of boilerplate. In Visual Basic, you drew the button on the form with your mouse, double-clicked it, and typed:

**[VISUAL: Code appearing on screen:]**

```vb
Sub Command1_Click()
    MsgBox "You clicked me!"
End Sub
```

That's it. Three lines. You hit F5, the program ran, the button worked. The gap between having an idea and seeing it work on screen was shorter than it had ever been in the history of programming.
~~~~

## Production delivery — this batch

**Media/source production: produced. Publication status: blocked.**

[Individual delivery inventory](../../assets/diagrams/DIA-01/delivery.json) · [QA and remaining decisions](../../assets/diagrams/DIA-01/qa.md) · [Live state](../../assets/diagrams/DIA-01/state.json)

The work order above remains unchanged. See the package gallery for rendered previews. Production does not clear an unresolved historical or editorial gate.
