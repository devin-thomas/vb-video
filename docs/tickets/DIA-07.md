---
id: "DIA-07"
title: "BuildDeck nested loops: 4 suits \u00d7 13 ranks"
owner_role: "motion"
original_family: "T1-D07"
priority: "required"
production_status: "planned"
release_gates: []
---

# DIA-07 — BuildDeck nested loops: 4 suits × 13 ranks

## Assignment and boundaries
**One ticket, one independently reviewed asset package.** Make the order of the nested loops visible and tie each grid cell to its array index.

**Original plan family:** `T1-D07` — Deck building nested-loop animation (4×13 grid)

**Primary owner role:** motion. **Type:** motion. **Script section(s):** 7. **Scope priority:** required.

**Method:** Offline HTML/CSS/inline SVG with deterministic JavaScript seeking; exported review MP4 and PNG stills.

**Owned write paths:** `assets/diagrams/DIA-07/`. Read other assets as dependencies; do not edit them. Original `sources/` files are read-only. The intended products below have not yet been generated by this pack.

## Required inputs and source anchors
Read this ticket, [AGENTS.md](../../AGENTS.md), the [production bible](../PRODUCTION_BIBLE.md), and the [output contract](../OUTPUT_CONTRACT.md). Relevant originals are included at the end of this ticket, so the work does not depend on a vague chat memory.

- `ASSET_PLAN.md:22–22` ([original](../../sources/ASSET_PLAN.md))
- `SCRIPT.md:239–279` ([original](../../sources/SCRIPT.md))
- `Program.vb:97–111` ([original](../../sources/Program.vb))

**Integration dependencies:** [OPS-01](OPS-01.md), [CODE-04](CODE-04.md)

Source discovery, content preparation, and internal-proof work may proceed while a framing or review dependency is pending. Do not mark the final dependency-consuming output reviewed before its inputs are accepted. Release clearance is separate from a dependency’s proof being usable.

## Creative and technical requirements
1. Use exactly four columns in order S,H,D,C and thirteen rows in ascending rank 2…14.
2. Traverse rank-major order: 2S,2H,2D,2C,3S,…,AC. Never transpose the outer and inner loops.
3. Show Rank, SuitIndex, Index, and the corresponding grid cell. Index starts at 0; the final occupied index is 51, then Index becomes 52.
4. Preserve Dim/For/Next spelling in any excerpt; pair colored brackets with text labels, not color alone.

## Exact copy / source payload
~~~~text
For Rank = 2 To 14
For SuitIndex = 0 To 3
52 cards = 13 ranks × 4 suits
~~~~

Text explicitly described as proposed in this ticket is an authored production choice, not an extra fact from the source. Otherwise preserve supplied wording/code. Do not silently rewrite the narration to make a visual easier to produce.

## Composition, states, and local timing
1. 0–3 s: empty labeled 4×13 grid.
2. 3–8 s: walk all four suits of rank 2 slowly.
3. 8–14 s: accelerate the middle ranks without skipping recorded states.
4. 14–18 s: show final Ace row and Index=52 completion state.

Use 1920×1080 with the shared safe area and readable system fonts. Motion uses 30 fps and deterministic `renderAt(seconds)`; timing here is a proposed local edit, not final narration sync. Every named state/cutdown needs a concrete file and mapping in delivery.json. Still assets hold for editorial timing; do not invent animation just because the common renderer can animate.

**Named variants:** `full-grid`, `first-rank-closeup`

## Deliverables
Paths below are relative to `assets/diagrams/DIA-07/`. Keep editable input, clean original, rendered view, and evidence separate. Globs stand only for genuine source/native-extension or multiple raw-capture cases. The output contract describes their contents.

- `delivery.json`
- `qa.md`
- `evidence/provenance.json`
- `evidence/source-excerpts.md`
- `src/index.html`
- `src/timeline.json`
- `exports/preview.mp4`
- `exports/poster.png`
- `exports/keyframes/start.png`
- `exports/keyframes/middle.png`
- `exports/keyframes/end.png`
- `exports/full-grid.png`
- `exports/first-rank-closeup.png`

Also maintain `state.json` in this folder. Write an exact local reproduction command and installed tool versions into qa.md/toolchain metadata. Record named cutdown timing and all generated filenames. Never create empty placeholder media and claim it satisfies these paths.

## Acceptance checks
- [ ] All 52 unique rank/suit combinations are filled exactly once.
- [ ] Final cell is Ace of Clubs at index 51.
- [ ] Next SuitIndex is visibly inner; Next Rank is visibly outer.
- [ ] The ordered build is not mislabeled as shuffled or dealt.
- [ ] Every required output is present and corresponds to this ID; no filename/asset swap occurred.
- [ ] All claimed tests actually ran, and manual visual inspection is recorded at full size and 720p where applicable.
- [ ] Sources, authored additions, original media, transformations, and rights/claim status are distinguishable.
- [ ] No unwanted UI copy, private information, remote font/CDN dependency, or fabricated evidence appears.
- [ ] The assigned dependency and release-gate statuses are reported truthfully.

## Editorial, authenticity, and access gates
No specific editorial register item is assigned. New outside claims or third-party materials still require evidence and rights review.

Do not create accounts, accept new terms, pay, bypass access controls/CAPTCHAs, remove watermarks, publish, or place secrets in files. Use the relevant [human handoff](../handoffs/README.md). If the local environment lacks a renderer/capture tool/SDK, report the exact missing capability and keep partial source work explicitly partial. Do not convert a suggested source into “found” without checking it.

## Completion report
Return this ID, concrete output paths, variant/duration information, exact provenance, tests and actual results, remaining blockers, production status, and release status. Update only your own state file. A URL-only scout, source-only draft, historical proof with unresolved rights, and editor-ready approved media are different outcomes.



## Embedded source context (unchanged excerpts)
### ASSET_PLAN.md:22–22

~~~~text
| 7 | Deck building nested-loop animation (4×13 grid) | HTML grid filling in |
~~~~
### SCRIPT.md:239–279

~~~~text
Next, we need a deck. Fifty-two cards: thirteen ranks times four suits.

```vb
Sub BuildDeck(ByRef Deck() As Card)
    Dim Suits() As Char = {"S"c, "H"c, "D"c, "C"c}
    Dim Rank As Integer
    Dim SuitIndex As Integer
    Dim Index As Integer

    Index = 0
    For Rank = 2 To 14
        For SuitIndex = 0 To 3
            Deck(Index).Rank = Rank
            Deck(Index).Suit = Suits(SuitIndex)
            Index = Index + 1
        Next SuitIndex
    Next Rank
End Sub
```

Two nested `For` loops. The outer loop walks through ranks 2 to 14. The inner loop walks through the four suits. We drop each card into the array at the current index and bump the index. After both loops finish, we have a perfectly ordered 52-card deck.

**[VISUAL: Animated grid filling in — 4 columns (suits) × 13 rows (ranks), each cell lighting up as the loop hits it.]**

Let's pause and look at a few VB-specific things here.

First: `Sub`. Not `void`, not `function` — `Sub`. Short for "subroutine." In Visual Basic, there's a hard distinction between a `Sub`, which does something and doesn't return a value, and a `Function`, which does something and gives you back a result. In C-family languages, a function that returns nothing is just a function with a `void` return type. In VB, it's a completely different keyword.

**[VISUAL: Side-by-side comparison:]**
```
C#:    void BuildDeck(Card[] deck) { ... }
VB:    Sub BuildDeck(ByRef Deck() As Card) ... End Sub
```

Second: `ByRef`. See that in the parameter list? In VB, you had to explicitly say whether a parameter was passed by reference or by value. `ByRef` means the subroutine receives a pointer to the original — changes are visible to the caller. `ByVal` means it gets a copy. In classic VB4, the default was `ByRef` for everything, which was efficient but dangerous. It meant you could accidentally modify data the caller didn't expect you to touch.

**[VISUAL: Diagram — ByRef: arrow pointing from sub back to caller's variable. ByVal: a copy being made, arrow pointing to a separate box.]**

Third: look at the `For` loop syntax. `For Rank = 2 To 14`. Then at the end: `Next Rank`. That `Next Rank` is closing the loop and telling you what variable is being incremented. If you're used to C-style `for (int i = 0; i < 14; i++)`, this is going to look almost quaint. But there's something kind of nice about it — when you have nested loops, each `Next` names its variable, so you can see at a glance which loop is ending.

**[VISUAL: The nested loops with colored brackets showing which `Next` closes which `For`.]**
~~~~
### Program.vb:97–111

~~~~text
    Sub BuildDeck(ByRef Deck() As Card)
        Dim Suits() As Char = {"S"c, "H"c, "D"c, "C"c}
        Dim Rank As Integer
        Dim SuitIndex As Integer
        Dim Index As Integer

        Index = 0
        For Rank = 2 To 14
            For SuitIndex = 0 To 3
                Deck(Index).Rank = Rank
                Deck(Index).Suit = Suits(SuitIndex)
                Index = Index + 1
            Next SuitIndex
        Next Rank
    End Sub
~~~~

## Production delivery — this batch

**Media/source production: produced. Publication status: unreviewed.**

[Individual delivery inventory](../../assets/diagrams/DIA-07/delivery.json) · [QA and remaining decisions](../../assets/diagrams/DIA-07/qa.md) · [Live state](../../assets/diagrams/DIA-07/state.json)

The work order above remains unchanged. See the package gallery for rendered previews. Production does not clear an unresolved historical or editorial gate.
