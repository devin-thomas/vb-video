# Devin's notes on the first cut (2026-09-16)

Review of `build/review-1080p60-mixed.mp4` (42:00, Session 9). Devin's verdict so far: about 80% there. Notes are collected here as they arrive; nothing is fixed until the review is complete and the fix plan is agreed. Then: plan, fix, re-render.

| # | Note | Where | Producer's first read of the cause |
|---|---|---|---|
| 1 | Music is too loud in every spot; quieter throughout. | Whole mix | Bed gains in `narration/music-plan.json` are -8 to -15 dB with `loudnorm` to -18 LUFS before the gain; the sidechain duck (ratio 6) leaves the beds forward under speech. Expect a global drop of several dB plus a deeper duck. |
| 2 | Songs should fade out fully before the next one starts, no crossfade. | Every bed change | Each bed fades out over its last 3 s and the next fades in over 2 s from the same second, and the chapter stings in `narration/sfx-music.json` land on top of the join, so the join reads as a crossfade. Needs a gap: fade out to silence, a beat of nothing, then the next bed. |
| 3 | The edit goes back and forth between two clips or rotates through a few; needs stabilising, or more assets where that motion is wanted. | Several sections | `build_timeline.py` splits a beat with several cues equally between them, and a beat with no cue repeats the previous beat's whole visual list, so the same two or three assets alternate across consecutive beats. Fix in the builder: hold one visual per beat unless the script cues a change, and carry only the last visual forward. Identify the sections where it happens from the timeline. |
| 4 | The intro shows the PowerShell War run; it should show the VB code scrolling instead. | Cold open, S01-B01 | The script cue is "Terminal output scrolling", mapped to TERM-04 (recording R4). Devin wants the code scroll-through (TERM-03, recording R5) there instead. One-line override in the timeline builder; TERM-04 keeps its later slot at 12:xx (S12-B01). |
| 5 | Real, representative code for the 73-line Windows 16-bit "Hello World" the narration mentions. | Section 2, "A 73-line C Hello World for Windows" cue | REF-02 currently fills the cue. Check what it actually shows against the narration's claim; if it is not the genuine Petzold-style Windows 3.x program at full length, produce a code asset that is (the real listing, scrolling, readable), and treat it as a new or revised ticket. |
| 6 | At about 8:45–9:00 the narration says an undeclared variable is created as a Variant; show it. Then show a variable being set and a typo'd name being used: `Nintendo` as the correct name, `Nintendont` as the misspelling. | Section 4, around 8:45–9:00 | No asset carries this today; the beat holds the previous visual. New code asset(s): one card showing the undeclared name silently becoming a Variant, one showing `Nintendo` set and `Nintendont` used, with the consequence (silent new variable, and what `Option Explicit` does about it). Names exactly as Devin gave them. |
| 7 | A unique sound effect must have the music duck sharply to the floor (or to zero) right before it, play in full, then the music returns. The Windows start-up chime clashed with the bed; possibly another clash at a bed change, unclear whether an effect was involved. | Effect cues (chime at 4:58; shuffles 9:12, 9:32, 15:54; slaps 21:43, 22:40) | Effects are mixed in after the duck and the bed is only ducked by the voice. Change the mix so each effect cue drives its own duck: bed to the floor a fraction of a second before the effect, hold for the effect's full length, ramp back. Check the bed joins for the second clash (relates to note 2). |
| 8 | The shuffle stock clip around 9:30 pauses for several seconds and never actually shuffles on screen. | About 9:30, BROLL-01 at S05-B02 | The clip is shorter than its slot, so the renderer holds its last frame. Either trim the slot to the clip's real shuffling action, choose the in/out points where the shuffle happens, or loop the action instead of freezing. |
| 9 | The rules explanation must have exact visuals: an ace beating a king on screen when it is said, a war happening when it is said, and no unrelated card animations before the narration reaches them. Today the animations play, then the narration describes them, then they repeat, with stock clips mixed into the explanation. Stock clips belong at the start or end of the technical stretch, not inside it; this stretch is locked to the card animations and game-logic visuals only. | Section 5, "The Card Game War: Rules in 60 Seconds", about 9:08–10:42 | DIA-02's preview is one continuous animation, repeated across four cues, with BROLL-01/BROLL-02 placed inside the stretch. Needs beat-accurate motion: cut DIA-02 (or re-render it) into the moments the script names, placed at the words, and move the two stock clips to the head or tail of the section. Likely a DIA-02 rework ticket plus timeline in/out points per beat. |
| 10 | The "shuffle" stock clip is a deck cut, not a shuffle. Replace with an actual shuffle; a video-game or animated shuffle is fine. | About 9:30, BROLL-01 | Retire BROLL-01 from the shuffle cue. Preferred route: an animated shuffle we render ourselves (riffle or overhand, card system from `assets/shared/`), so no new rights work. Supersedes the trim idea in note 8. |
| 11 | The rules-explanation animations may stay on screen longer so they last through the narration. | Section 5 | Pairs with note 9: instead of repeating the clip, let each named moment hold (or play slowly) for the whole beat. |
| 12 | On the Card Structure code card, arrows cut through words to point at `Dim`. | Section 6, CODE-02 (about 10:42–11:30) | Annotation overlay collides with the code text. Re-route the arrows to enter from the margin or use a highlight bar instead of arrows; re-render CODE-02 and check at 1080p and 720p. |
| 13 | At roughly 13:00 the comparison card says "C#" but the `int rank; char suit;` example is the same in C, C++ and C#; label it "C / C++ / C#". Every code comparison must visibly list the languages the example is valid in when it is not specific to one; `void BuildDeck` and others likely apply. | All CMP cards (CMP-01 to CMP-08), around 13:00 onward | Audit each CMP card's left-hand snippet: decide which of C, C++, C#, Java it is valid in and set the header to the full list. Re-render the affected cards. CMP-01 (`int rank; char suit;`) and the `void BuildDeck` card are confirmed cases. |
| 14 | "Sub = a function that returns nothing; Function = a function that returns a value" needs a visual or animation, and a more obvious pairing: `void HelloWorld` and `int Add` on the C-style side against `Sub HelloWorld` and `Function Add` on the VB side. | Section 7, the Sub/Function beat (CMP-02 area) | New or reworked comparison asset with the two pairs side by side, ideally animated (the return value appearing on the Function/`int` row and nothing on the Sub/`void` row). Header lists the languages per note 13. |
| 15 | The for-loop explanation says "i" in the narration but the on-screen variable is `rank`. Make the screen say `i`. | Section 7, the `For` loop beat (CMP-03 / CODE card around 13:30–14:30) | Rename the loop variable on the card(s) to `i`. The narration is final, so the asset follows the voice. Check the same card is not also quoted as a literal excerpt of Program.vb (the validator treats code cards as excerpts); if it is, the card becomes a teaching example and is labelled as such. |
| 16 | The ByRef/ByVal visual at 15:12 does not communicate whether a call gets a copy or the variable itself, or why it matters. Show it with a couple of lines of code instead of the diagram, and optionally C++ with `&` as a comparison. | About 15:12, DIA-0x ByRef/ByVal diagram in section 8 | Replace the diagram with a short animated code demo: a variable set, a `ByVal` call that changes its parameter (caller unchanged), a `ByRef` call that changes it (caller changed), with the values shown updating beside the code. Optional third column: C++ `void f(int& x)`. New asset; retire the diagram from the timeline. |
| 17 | The card faces at 15:58 are a single glyph centred on each card, so they all read as aces at a glance. They should look like regular playing cards. | About 15:58 (DIA-06 shuffle / hand animation, and any other asset using the shared card face) | The shared card system in `assets/shared/` draws one centre pip. Rework the face: corner index and suit plus the standard pip layout for 2–10, court letter for J/Q/K, single pip only for the ace. Every asset built on the shared cards re-renders (DIA-02, DIA-06, DIA-08, DIA-09 and the new shuffle from note 10). |
| 18 | The VB declaration and the C89 mention around 16:30 deserve code snippets on screen. | About 16:30, section 8 (arrays as queues), the `Dim`/C89 beats | No visual carries these beats today. Two small code cards: the VB declaration as written in Program.vb, and the C89 equivalent (declare-at-top / fixed-size array) it is being contrasted with, headed with the language per note 13. |
| 19 | Around 17:00 the narration says "Q++" instead of "C++". | About 17:00, section 8 (S08-B02 starts 16:52; QA already showed "c plus plus" heard as something else on that take) | A pronunciation slip by the voice, not the words. Re-synthesize that beat with the spoken form adjusted so the letter lands (for example "see plus plus"), QA it, and re-select. The written script does not change. Sweep the QA diffs for any other beat where "C++" or "C#" was not heard as expected. |

More notes to follow while Devin keeps watching.

## Tickets opened from these notes (2026-09-16)

| Note | Ticket | What it does |
|---|---|---|
| 1, 7 | [ASM-01](../docs/tickets/ASM-01.md) | Quieter beds throughout; every effect cue ducks the bed to the floor for the effect's full length |
| 2, 7 | [ASM-02](../docs/tickets/ASM-02.md) | Bed changes fade to silence with a gap; stings placed clear of joins; no crossfades |
| 3, 4, 8, 9, 10, 11 | [ASM-03](../docs/tickets/ASM-03.md) | Timeline: one visual per beat, no return trips, code scroll-through in the cold open, section 5 locked to the card animations at the words, stock clips at the edges, no frozen motion clips |
| 19 | [NAR-01](../docs/tickets/NAR-01.md) | Re-read the beats where "C++" or "C#" is mispronounced; spoken form only |
| 5 | [REF-02](../docs/tickets/REF-02.md) revision 2 | A complete 73-line Win16 Hello World in C, scrolling slowly |
| 6 | [CODE-26](../docs/tickets/CODE-26.md) | Undeclared `x` created on first use as a Variant |
| 6 | [CODE-27](../docs/tickets/CODE-27.md) | `Nintendo` set, `Nintendont` used: the silent new variable, then the Option Explicit error |
| 12 | [CODE-02](../docs/tickets/CODE-02.md) revision 2 | Arrows no longer cross the code text |
| 13 | [CMP-01](../docs/tickets/CMP-01.md) … [CMP-08](../docs/tickets/CMP-08.md) revision 2 | Left-column headers list every language the snippet is valid in (C / C++ / C#, C / C++ / C# / Java, C++ / C# / Java, or C# with a margin note) |
| 14 | [CMP-02](../docs/tickets/CMP-02.md) revision 2 | Reworked into `void HelloWorld` / `int Add` versus `Sub HelloWorld` / `Function Add`, animated |
| 15 | [CMP-03](../docs/tickets/CMP-03.md) revision 2 | Loop variable becomes `i` on screen, then the example transforms into (or points at) the real `Rank` loop |
| 16 | [DIA-04](../docs/tickets/DIA-04.md) revision 2 | ByRef/ByVal shown as a two-line code demo with values updating; optional C++ `&` column |
| 17 | [OPS-01](../docs/tickets/OPS-01.md) revision 2 | Shared card faces redrawn as regular playing cards; dependents (DIA-02, 03, 05, 06, 07, 08, 09, CARD-01, MOCK-01) re-render afterwards |
| 18 | [CODE-28](../docs/tickets/CODE-28.md), [REF-05](../docs/tickets/REF-05.md) | The Program.vb Dim block, and a C89 declarations-first card |
| 9, 11 | [DIA-02](../docs/tickets/DIA-02.md) revision 2 | Rules animation re-cut into named, holdable cutdowns with the key moment's second recorded for placement on the word |
| 8, 10 | [DIA-15](../docs/tickets/DIA-15.md) | Animated riffle shuffle replacing the stock deck-cut clip |

| 17:00–end pass 1 | [DIA-05](../docs/tickets/DIA-05.md) revision 2 | Array-as-queue animation cut into `append`, `draw-shift`, `cost` moments that land on the words |
| 17:00–end pass 2 | [CMP-09](../docs/tickets/CMP-09.md) | `Queue<Card>` / `std::queue` / 1995 VB array, three columns |
| 17:00–end pass 3 | [FACT-07](../docs/tickets/FACT-07.md) | The four recorded runs as one table |
| 17:00–end pass 4 | [DIA-08](../docs/tickets/DIA-08.md) revision 2 | `endgame` variant with the real 2-vs-50 numbers, panels on the sentences |
| 17:00–end pass 5 | [FACT-08](../docs/tickets/FACT-08.md) | Two endgames side by side (replaces the mis-mapped CMP-02 at 26:31) |
| 17:00–end pass 6 | [CODE-29](../docs/tickets/CODE-29.md) | "Reads like English": the two quoted VB lines |
| 17:00–end pass 7 | [DIA-12](../docs/tickets/DIA-12.md) revision 2 | Windows-vs-Mac table revealed row by row |
| 17:00–end pass, must-fix | [NAR-01](../docs/tickets/NAR-01.md) addendum | Stage directions read aloud at 25:43 and 26:31: cue parser fix, beats removed |
| 17:00–end pass, placements | [ASM-03](../docs/tickets/ASM-03.md) addendum | Every approved placement fix as a checklist item |

Devin approved all seven proposals on 2026-09-16 ("Those are all great. Ticket them all").

**Status 2026-09-16 (late):** every ticket above is produced, merged and release-approved; the second cut is rendering. See docs/INTEGRATION_LOG.md, Session 10.

Dispatch order: OPS-01 first (every card asset waits on the faces), then DIA-02, DIA-15 and the other assets in parallel, NAR-01 any time, ASM-01 and ASM-02 any time (mix only), ASM-03 last (it places everything), then the full re-render.

## Producer's pass over 17:00–end (2026-09-16), applying Devin's rules — proposals awaiting Devin

Method: timeline, beat text and QA data checked against the rules above (visuals land on the words, no return trips, code on screen when code is spoken, languages labelled, stock clips at the edges, nothing frozen or stale), plus frame checks at twelve points.

**Must fix, not taste**
- 25:43 (S12-B05, 24 s) and 26:31 (S12-B08, 12 s): Aiden reads stage directions aloud ("Panel 1 ... Panel 4", "TERM-04: 2 cards → play 10 ..."). Multi-line VISUAL cues were parsed as narration. Added to NAR-01.

**Placement fixes the producer will make under ASM-03 unless Devin objects** (no new assets)
- 19:31 a 0.7 s flash of the rules animation between two code cards; hold the code instead.
- 20:10 the narration moves to `&` concatenation while the flowchart and CODE-14 sit frozen for 37 s; CMP-06 (concatenation) moves here from 20:45; CMP-08 (line continuation) stands alone at 20:45.
- 21:14 four code cards in 13 s; CODE-15 alone. 22:14 / 22:21 / 22:26 "One… Two… Three…" get CODE-17 / CODE-18 / CODE-19 on those words instead of the pot card frozen for 37 s.
- 23:52 "Ace against Ace, then Queen against Queen": one visual, the terminal lines of that double war (TERM-02's first-war frame), not four cycling.
- 24:42 "rewind to that first run… here's how it ended": show the ending (TERM-04's end frame or the recording's last seconds), not the recording restarting on a blank terminal.
- 27:29–27:54 "put a GUI on it… form designer… PictureBox… Deal button": MOCK-01 (the VB4 War GUI mockup) from 27:29; the Solitaire/Hearts/FreeCell screenshots move to 28:18 where Solitaire is named.
- 30:16 Visual C++ / MFC shows the lineup still while the MFC code card sits under Delphi at 30:50. Each tool lands on its own name: REF-01 at 30:16, HIST-12 (Delphi IDE) at 30:50, HIST-15 (PowerBuilder DataWindow) at 31:51, HIST-16 (Java AWT) at 32:14.
- 33:19 "never shipped VB for the Mac": XTRA-10 (that card) here instead of 33:51; HIST-13 (CodeWarrior IDE) at 33:51 under CodeWarrior; HIST-14 (NeXT cube) at 34:39 when NeXT is named.
- 29:16–30:01 the download dialog (9 s clip) freezes for 38 s; after it plays, cut to MOCK-01 for "make your own Solitaire".
- 37:40 HIST-03 (VB6 IDE) alone on the VB6 sentence; BROLL-03 alone on "software written a quarter century ago".
- 39:20 "organized an actual petition": the petition page (XTRA-17) moves here from 39:32.
- Outro: 40:47 "in about 280 lines" → the code scroll-through (TERM-03); 41:04 "run dotnet new console -lang VB" → TERM-01 (that exact capture); 41:32 end card alone, not sharing with the terminal.

**Devin's call: new or reworked assets**
1. 17:20–18:24 arrays as a queue: make DIA-05 beat-accurate like section 5 — append state on "drop it at position Count" (17:37), draw-and-shift on "take the card at position zero, then shift" (17:44), code lines on their sentences. Today the diagram and code alternate.
2. 17:00 `Queue<Card>` / `std::queue` / 1995 VB arrays: a three-column card (C# / C++ / VB 1995) so the code is on screen when named (new CMP-09).
3. 24:06 "every run is different… 150 rounds… 2,000": one summary card of the four recorded runs (rounds, wars, winner) instead of four captures cycling in 14 s (new FACT-07).
4. 25:43 the endgame walk-through (2 cards vs 50, both play a 10, burn 1, 4-card pot): DIA-08 gets an `endgame` variant with those real numbers, panels landing on the words (DIA-08 revision).
5. 26:31–27:18 the two-endgame side-by-side is currently the C#/VB BuildDeck card for 47 s (wrong asset). New two-column card: Run 1 (617 rounds): 2 cards → play 10 → burn 1 → empty → 4-card pot | Run 4 (2,008 rounds): 3 cards → play 8 → burn 2 → empty → 6-card pot (new FACT-08).
6. 37:13 `If PlayerScore > HighScore Then` and `For Each Item In Collection` are quoted while an invoice app screenshot shows: a "reads like English" code card (new CODE-29); the VB application screenshots move to 37:04.
7. 35:25–36:32 the Windows-vs-Mac table holds for 63 s: reveal it row by row, console row at 35:25, GUI row ("an afternoon vs a week") at 35:50 (DIA-12 revision).
8. Cards at 17:25, 21:50, 22:45 show the same single-pip faces; OPS-01's revision covers them.
